## Device Configurator UI

This folder hosts the reusable UI components that render device configuration
parameters for sensorimotor setup. It mirrors the controller_configurator
parameter editors so Brain Visualizer can present structured input fields
instead of raw JSON.

Dependencies:
- Parameter data classes in `Data/`
- Parameter editor scenes and scripts in `Parameters/`
