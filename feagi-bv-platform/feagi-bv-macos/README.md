# feagi-bv-macos

Brain Visualizer binaries for macOS (universal: arm64 + x86_64).

This package is automatically installed when you run `pip install feagi-bv` on macOS.

**Do not install directly** - use `pip install feagi-bv` instead.
